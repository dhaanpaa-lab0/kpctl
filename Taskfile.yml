version: 3

use_gitignore: true

tasks:
  default:
    cmd: task -l
  clean:
    desc: Clean up distribution folder
    cmds:
      - rm -rfv dist
  build:
    desc: Build the project
    deps:
      - clean
    cmds:
      - go build -o "dist/kpctl.$(go env GOHOSTARCH).$(go env GOHOSTOS)"
